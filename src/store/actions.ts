import { IState, IActions, IGetters, IMutations, Actions } from "@/store/types";

const actions: Actions<
IState,
IActions,
IGetters,
IMutations
> = {
    // async commitCrickedButton({ commit }, payload) {
    //     commit('setCrickedButton', payload)
    //     },
}

export default actions;